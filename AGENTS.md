# AIGuide 项目维护规则

## 定位

中文 AI 学习网站，React + Vite + JavaScript，当前以静态交付为主。先读 README、docs/README.md 与 docs/ROADMAP.md，再定位修改。

## 工程

- 使用 Node 24 LTS、pnpm 11.19.0；依赖以锁文件为准。
- 修改前检查 Git 根目录和状态，不把外层目录的文件带入本项目。
- 沿用现有模块边界，避免无关重构和新的单体文件。
- 默认不增加运行时依赖；确有必要时说明用途并更新锁文件。
- 不把个人机器的绝对路径、运行时路径或凭据写进可复用代码与工程指南。

## 产品与内容

- 保留已确认的书页标识、品牌原色、中文排版与公司卡片。
- 面向新手只突出网页版、桌面版、API、模型；进阶入口在教程内部解释。
- 修改课程前查看 src/data/index.js 的覆盖顺序，确认最终数据。
- 官方资料核对、人工参考、界面示意和账号内实测分别标识。
- 不编造日期、分数、产品功能或验证记录。
- 课程 ID、section-N、学习记录键和备份结构需要保持兼容。
- 新增章节类型同时考虑互动版和独立 HTML 阅读版。

## 验证与交付

- 常规检查运行 pnpm check；联网审计和真实产品操作单独记录。
- UI 改动检查桌面、平板和手机及对应交互；不只依赖构建。
- 临时截图、浏览器脚本和报告放项目外或 .local/，不混入源码提交。
- 更新对应文档与待办，说明已验证和未验证的范围。
- 只有用户明确要求提交时才 git commit；只有明确要求推送时才 git push。
- 未收到发布目标与指令，不自动部署、开通付费服务或创建定时任务。
