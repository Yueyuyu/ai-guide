# 贡献与维护说明

## 开始工作

先读 [README](README.md)、[架构](docs/engineering/ARCHITECTURE.md) 和[当前待办](docs/ROADMAP.md)。确认 Git 根目录指向本项目，查看工作区已有改动。

新工作分支使用 `codex/简短主题`；不把多个无关问题混成一次提交。示例：`codex/lesson-doubao-evidence`。

## 改动约定

- 保持现有蓝色书页 Logo、中文教学风格和品牌原色，未经需求确认不重新设计全站。
- 公司下按网页版、桌面版、API、模型组织；教程按实际使用端写步骤。
- 内容写明具体输入、操作、结果核对、失败处理和来源。
- 优先沿用现有依赖。新增依赖需要说明已有能力为什么不足，并更新锁文件。
- 记录 ID、章节位置、备份格式的变化需要兼容方案。
- 修改正文时检查互动版与独立阅读页；修改共享组件时检查受影响入口。
- 不提交密钥、账号备份、私人对话或测试时生成的个人成果。

## 提交前

```sh
pnpm check
git status --short
git diff
```

UI 改动补充[浏览器验收](docs/engineering/QUALITY.md)。外链或产品实测失败如实记录，不通过删除检查或刷新日期掩盖。

提交信息采用 `<type>: <中文描述>`，例如 `docs: 补充教程来源核对流程` 或 `fix: 修复手机端成果按钮溢出`。

若由 AI 操作 Git，只有会话明确要求提交或推送时才执行对应动作。推送目标不明确时先完成本地改动和验证。

## Pull Request

描述具体问题与修改后的行为，列出有效检查与未测范围。正文更新提供来源，UI 更新提供必要截图；不要复制完整对话或重复所有命令日志。

模板见 [.github/pull_request_template.md](.github/pull_request_template.md)。Issue 模板覆盖功能问题和教程纠错；仓库尚未推送前，这些是本地文件，不是已创建的 Issue。

## 许可

项目原创代码、文档和练习材料采用 [MIT License](LICENSE)。提交原创贡献时沿用该许可；引入第三方代码或素材时保留来源及许可说明，不将第三方商标、资料和依赖改称 MIT 资产。见 [NOTICE.md](NOTICE.md)。
